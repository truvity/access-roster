# A key under oauthClient.secret does not know must fail the render, not be ignored.
# error: at '/oauthClient/secret': additional properties
issuerURL: https://issuer.example
oauthClient:
  secret:
    namespace: elsewhere
