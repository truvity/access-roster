# A key in a catalogue App does not know must fail the render, not be ignored.
# error: at '/githubApps/catalogue/0': additional properties
issuerURL: https://issuer.example
githubApps:
  catalogue:
    - id: releases
      org: acme
      permissions: {contents: write}
      webhook: https://hooks.example
