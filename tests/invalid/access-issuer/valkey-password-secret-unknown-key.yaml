# A key under valkey.passwordSecret does not know must fail the render, not be ignored.
# error: at '/valkey/passwordSecret': additional properties
issuerURL: https://issuer.example
valkey:
  passwordSecret:
    keyName: password
