# The API path is appended to the address.
# error: at '/secretManagers/0/address': 'https://openbao.example.private/v1' does not match pattern
# A base URL carrying a path of its own builds calls nobody wrote.
issuerURL: https://issuer.example
secretManagers:
  - name: kernel
    address: https://openbao.example.private/v1
    namespaces:
      - name: devel
