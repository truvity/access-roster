# A key under oauthClient.secret.keys does not know must fail the render, not be ignored.
# error: at '/oauthClient/secret/keys': additional properties
issuerURL: https://issuer.example
oauthClient:
  secret:
    keys:
      id: client-id
