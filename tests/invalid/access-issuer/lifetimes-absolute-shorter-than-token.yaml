# An access token cannot outlive the session that grants it.
# error: lifetimes.absolute (30m) must be at least lifetimes.token (1h)
issuerURL: https://issuer.example
lifetimes:
  token: 1h
  absolute: 30m
