# A key under signingKey.certificate does not know must fail the render, not be ignored.
# error: at '/signingKey/certificate': additional properties
issuerURL: https://issuer.example
signingKey:
  certificate:
    algorithm: ECDSA
