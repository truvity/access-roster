# A key under client does not know must fail the render, not be ignored.
# error: at '/client': additional properties 'id' not allowed
exposure:
  hostname: console.example
  gateway:
    name: internal
    namespace: gateway-system
  backend:
    name: console
  allow:
  - console:operator
issuer:
  url: https://issuer.example
client:
  secret:
    name: console-client
  id: x
session:
  valkey:
    address: valkey.example.svc:6379
  cookieSecret:
    name: console-cookie
