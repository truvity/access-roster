# access-issuer — configuration and endpoints

This page merged into [configuration.md](configuration.md): every chart
value, the endpoints the issuer serves, the overlay format, and the
objects the service writes are there now, in one table and one list
instead of two overlapping ones.

- [Values](configuration.md#values)
- [Endpoints](configuration.md#endpoints)
