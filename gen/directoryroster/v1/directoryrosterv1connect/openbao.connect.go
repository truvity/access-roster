// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: directoryroster/v1/openbao.proto

package directoryrosterv1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/truvity/access-roster/gen/directoryroster/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// SecretManagerServiceName is the fully-qualified name of the SecretManagerService service.
	SecretManagerServiceName = "directoryroster.v1.SecretManagerService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// SecretManagerServiceListSecretManagersProcedure is the fully-qualified name of the
	// SecretManagerService's ListSecretManagers RPC.
	SecretManagerServiceListSecretManagersProcedure = "/directoryroster.v1.SecretManagerService/ListSecretManagers"
	// SecretManagerServiceGetSecretManagerNamespaceProcedure is the fully-qualified name of the
	// SecretManagerService's GetSecretManagerNamespace RPC.
	SecretManagerServiceGetSecretManagerNamespaceProcedure = "/directoryroster.v1.SecretManagerService/GetSecretManagerNamespace"
	// SecretManagerServiceListSecretManagerReachProcedure is the fully-qualified name of the
	// SecretManagerService's ListSecretManagerReach RPC.
	SecretManagerServiceListSecretManagerReachProcedure = "/directoryroster.v1.SecretManagerService/ListSecretManagerReach"
)

// SecretManagerServiceClient is a client for the directoryroster.v1.SecretManagerService service.
type SecretManagerServiceClient interface {
	// ListSecretManagers returns every declared store with its namespaces,
	// each carrying the counts that summarise it. Viewer.
	//
	// The counts come from the same comparison GetSecretManagerNamespace
	// makes, so a summary can never disagree with the page it summarises.
	ListSecretManagers(context.Context, *connect.Request[v1.ListSecretManagersRequest]) (*connect.Response[v1.ListSecretManagersResponse], error)
	// GetSecretManagerNamespace returns one namespace: its groups with
	// what the store holds for each, the policies it carries, and the
	// doors that admit them. Viewer.
	GetSecretManagerNamespace(context.Context, *connect.Request[v1.GetSecretManagerNamespaceRequest]) (*connect.Response[v1.GetSecretManagerNamespaceResponse], error)
	// ListSecretManagerReach returns what one person can reach: for every
	// group they hold that a store admits, the namespace and the paths
	// that group's policy opens, and whether it may write them. Viewer.
	//
	// Assembled from the policy the STORE holds rather than from the one
	// the estate declares, because the gap between the two is what
	// somebody looking at this page is trying to find.
	ListSecretManagerReach(context.Context, *connect.Request[v1.ListSecretManagerReachRequest]) (*connect.Response[v1.ListSecretManagerReachResponse], error)
}

// NewSecretManagerServiceClient constructs a client for the directoryroster.v1.SecretManagerService
// service. By default, it uses the Connect protocol with the binary Protobuf Codec, asks for
// gzipped responses, and sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply
// the connect.WithGRPC() or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewSecretManagerServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) SecretManagerServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	secretManagerServiceMethods := v1.File_directoryroster_v1_openbao_proto.Services().ByName("SecretManagerService").Methods()
	return &secretManagerServiceClient{
		listSecretManagers: connect.NewClient[v1.ListSecretManagersRequest, v1.ListSecretManagersResponse](
			httpClient,
			baseURL+SecretManagerServiceListSecretManagersProcedure,
			connect.WithSchema(secretManagerServiceMethods.ByName("ListSecretManagers")),
			connect.WithClientOptions(opts...),
		),
		getSecretManagerNamespace: connect.NewClient[v1.GetSecretManagerNamespaceRequest, v1.GetSecretManagerNamespaceResponse](
			httpClient,
			baseURL+SecretManagerServiceGetSecretManagerNamespaceProcedure,
			connect.WithSchema(secretManagerServiceMethods.ByName("GetSecretManagerNamespace")),
			connect.WithClientOptions(opts...),
		),
		listSecretManagerReach: connect.NewClient[v1.ListSecretManagerReachRequest, v1.ListSecretManagerReachResponse](
			httpClient,
			baseURL+SecretManagerServiceListSecretManagerReachProcedure,
			connect.WithSchema(secretManagerServiceMethods.ByName("ListSecretManagerReach")),
			connect.WithClientOptions(opts...),
		),
	}
}

// secretManagerServiceClient implements SecretManagerServiceClient.
type secretManagerServiceClient struct {
	listSecretManagers        *connect.Client[v1.ListSecretManagersRequest, v1.ListSecretManagersResponse]
	getSecretManagerNamespace *connect.Client[v1.GetSecretManagerNamespaceRequest, v1.GetSecretManagerNamespaceResponse]
	listSecretManagerReach    *connect.Client[v1.ListSecretManagerReachRequest, v1.ListSecretManagerReachResponse]
}

// ListSecretManagers calls directoryroster.v1.SecretManagerService.ListSecretManagers.
func (c *secretManagerServiceClient) ListSecretManagers(ctx context.Context, req *connect.Request[v1.ListSecretManagersRequest]) (*connect.Response[v1.ListSecretManagersResponse], error) {
	return c.listSecretManagers.CallUnary(ctx, req)
}

// GetSecretManagerNamespace calls
// directoryroster.v1.SecretManagerService.GetSecretManagerNamespace.
func (c *secretManagerServiceClient) GetSecretManagerNamespace(ctx context.Context, req *connect.Request[v1.GetSecretManagerNamespaceRequest]) (*connect.Response[v1.GetSecretManagerNamespaceResponse], error) {
	return c.getSecretManagerNamespace.CallUnary(ctx, req)
}

// ListSecretManagerReach calls directoryroster.v1.SecretManagerService.ListSecretManagerReach.
func (c *secretManagerServiceClient) ListSecretManagerReach(ctx context.Context, req *connect.Request[v1.ListSecretManagerReachRequest]) (*connect.Response[v1.ListSecretManagerReachResponse], error) {
	return c.listSecretManagerReach.CallUnary(ctx, req)
}

// SecretManagerServiceHandler is an implementation of the directoryroster.v1.SecretManagerService
// service.
type SecretManagerServiceHandler interface {
	// ListSecretManagers returns every declared store with its namespaces,
	// each carrying the counts that summarise it. Viewer.
	//
	// The counts come from the same comparison GetSecretManagerNamespace
	// makes, so a summary can never disagree with the page it summarises.
	ListSecretManagers(context.Context, *connect.Request[v1.ListSecretManagersRequest]) (*connect.Response[v1.ListSecretManagersResponse], error)
	// GetSecretManagerNamespace returns one namespace: its groups with
	// what the store holds for each, the policies it carries, and the
	// doors that admit them. Viewer.
	GetSecretManagerNamespace(context.Context, *connect.Request[v1.GetSecretManagerNamespaceRequest]) (*connect.Response[v1.GetSecretManagerNamespaceResponse], error)
	// ListSecretManagerReach returns what one person can reach: for every
	// group they hold that a store admits, the namespace and the paths
	// that group's policy opens, and whether it may write them. Viewer.
	//
	// Assembled from the policy the STORE holds rather than from the one
	// the estate declares, because the gap between the two is what
	// somebody looking at this page is trying to find.
	ListSecretManagerReach(context.Context, *connect.Request[v1.ListSecretManagerReachRequest]) (*connect.Response[v1.ListSecretManagerReachResponse], error)
}

// NewSecretManagerServiceHandler builds an HTTP handler from the service implementation. It returns
// the path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewSecretManagerServiceHandler(svc SecretManagerServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	secretManagerServiceMethods := v1.File_directoryroster_v1_openbao_proto.Services().ByName("SecretManagerService").Methods()
	secretManagerServiceListSecretManagersHandler := connect.NewUnaryHandler(
		SecretManagerServiceListSecretManagersProcedure,
		svc.ListSecretManagers,
		connect.WithSchema(secretManagerServiceMethods.ByName("ListSecretManagers")),
		connect.WithHandlerOptions(opts...),
	)
	secretManagerServiceGetSecretManagerNamespaceHandler := connect.NewUnaryHandler(
		SecretManagerServiceGetSecretManagerNamespaceProcedure,
		svc.GetSecretManagerNamespace,
		connect.WithSchema(secretManagerServiceMethods.ByName("GetSecretManagerNamespace")),
		connect.WithHandlerOptions(opts...),
	)
	secretManagerServiceListSecretManagerReachHandler := connect.NewUnaryHandler(
		SecretManagerServiceListSecretManagerReachProcedure,
		svc.ListSecretManagerReach,
		connect.WithSchema(secretManagerServiceMethods.ByName("ListSecretManagerReach")),
		connect.WithHandlerOptions(opts...),
	)
	return "/directoryroster.v1.SecretManagerService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case SecretManagerServiceListSecretManagersProcedure:
			secretManagerServiceListSecretManagersHandler.ServeHTTP(w, r)
		case SecretManagerServiceGetSecretManagerNamespaceProcedure:
			secretManagerServiceGetSecretManagerNamespaceHandler.ServeHTTP(w, r)
		case SecretManagerServiceListSecretManagerReachProcedure:
			secretManagerServiceListSecretManagerReachHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedSecretManagerServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedSecretManagerServiceHandler struct{}

func (UnimplementedSecretManagerServiceHandler) ListSecretManagers(context.Context, *connect.Request[v1.ListSecretManagersRequest]) (*connect.Response[v1.ListSecretManagersResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("directoryroster.v1.SecretManagerService.ListSecretManagers is not implemented"))
}

func (UnimplementedSecretManagerServiceHandler) GetSecretManagerNamespace(context.Context, *connect.Request[v1.GetSecretManagerNamespaceRequest]) (*connect.Response[v1.GetSecretManagerNamespaceResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("directoryroster.v1.SecretManagerService.GetSecretManagerNamespace is not implemented"))
}

func (UnimplementedSecretManagerServiceHandler) ListSecretManagerReach(context.Context, *connect.Request[v1.ListSecretManagerReachRequest]) (*connect.Response[v1.ListSecretManagerReachResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("directoryroster.v1.SecretManagerService.ListSecretManagerReach is not implemented"))
}
